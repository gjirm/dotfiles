unbind C-b
set -g prefix C-a 
set -g mouse on
set -g default-terminal "screen-256color"

# split panes using § and -              
bind § split-window -h                   
bind - split-window -v                   
unbind '"'                  
unbind %
