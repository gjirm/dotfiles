unbind C-b
set -g prefix C-a 
set -g mouse on
set -g default-shell /usr/bin/zsh
set -g default-terminal "tmux-256color"

# split panes using § and -              
bind § split-window -h                   
bind - split-window -v                   
unbind '"'                  
unbind %
